// LangExtract Performance tests (skeleton)
// Intentionally skipped until implementation is ready

describe.skip('LangExtract Performance', () => {
  it('AC4 – throughput ≥ baseline − 5%', async () => {
    // TODO: Run N concurrent analyses baseline vs enabled and compare ops/sec
  })

  it('AC4 – memory overhead ≤ +10% p95', async () => {
    // TODO: Sample process memory usage during runs and compute p95 deltas
  })
})
